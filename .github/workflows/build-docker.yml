name: Build and Push VTK build image

on:
  workflow_dispatch:
    inputs:
      branch:
        required: true
        default: 'dev'
      push:
        required: false
        default: 'true'
      vtk:
        required: true
        default: '9.3.1'
      vtk_major:
        required: true
        default: '9.3'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.branch }}

      - name: Setup QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and Push image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: docker/Dockerfile
          platforms: linux/loong64
          push: ${{ github.event.inputs.push }}
          build-args: |
            vtk=${{ github.event.inputs.vtk }}
            vtk_major=${{ github.event.inputs.vtk_major }}
            python_bin=python3.13
            venv_dir=.build-vtk
          tags: |
            ghcr.io/loongson-cloud-community/cadquery-vtk-py313-build-base:latest
            ghcr.io/loongson-cloud-community/cadquery-vtk-py313-build-base:${{ github.event.inputs.vtk }}
          outputs: type=image,oci-mediatypes=true,compression=zstd,compression-level=3,force-compression=true
          cache-from: type=gha
          cache-to: type=gha,mode=max
